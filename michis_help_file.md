- CSS styles can be edited under _sass

- git add . + git commit -m "" + git push

- include images from obergurgl sleep

- compress all images of a folder with preview-tools-resolution to 72pixels

- LOCALLY: "jekyll serve"